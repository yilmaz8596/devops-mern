services:
  # --- Backend (Express + Mongoose) ---
  backend:
    build: ./server               # Build image from server/Dockerfile
    container_name: backend
    restart: unless-stopped       # Auto-restart on crash, unless manually stopped
    env_file: ./server/.env       # Inject PORT and MONGO_URI at runtime
    extra_hosts:
      - "host.docker.internal:host-gateway"  # Resolve host machine IP so container can reach host MongoDB
    ports:
      - "5000:5000"               # Expose API on host port 5000

  # --- Frontend (Vite + React) ---
  frontend:
    build:
      context: ./client
      args:
        VITE_API_URL: http://localhost:5000/api  # Baked into bundle at build time (Vite requires build-time injection)
    container_name: frontend
    restart: unless-stopped
    ports:
      - "5173:5173"               # Expose UI on host port 5173
    depends_on:
      - backend                   # Ensure backend container starts first